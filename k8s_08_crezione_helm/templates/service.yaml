apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s_08_crezione_helm.fullname" . }}
  labels:
    {{- include "k8s_08_crezione_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8s_08_crezione_helm.selectorLabels" . | nindent 4 }}
